import { SxProps } from "@mui/material";

interface CharacterBackendListProps {
  characterId: string,
  sx?: SxProps,
}

export default function CharacterBackendList({ characterId, sx }: CharacterBackendListProps) {
  

}